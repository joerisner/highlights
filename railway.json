{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "preDeployCommand": ["uv run fastapi --version"],
    "healthcheckPath": "/health",
    "healthcheckTimeout": 240
  }
}
